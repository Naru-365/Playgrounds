// Nomicierge 接続設定
// supabase/README.md の手順で取得した値を貼り付けてください。
// 値が空のままの場合、各画面はオフラインモード(localStorage のみ)で動きます。
var NOMI_CONFIG = {
  SUPABASE_URL: '',      // 例: 'https://xxxx.supabase.co'
  SUPABASE_ANON_KEY: '', // Settings → API → anon (public) key
  EVENT_ID: '',          // setup.sql 実行結果の EVENT_ID
};
